// import React, { useState } from "react";
// import { Button } from "react-bootstrap";
// import CallScreen from "./CallScreen"; // Import component CallScreen

// const CallButton = ({ roomId }) => {
//   const [showCallScreen, setShowCallScreen] = useState(false);

//   return (
//     <div className="text-center mt-3">
//       <Button variant="primary" onClick={() => setShowCallScreen(true)}>
//         Start Video Call
//       </Button>

//       {/* Hiển thị modal cuộc gọi */}
//       <CallScreen roomId={roomId} show={showCallScreen} onHide={() => setShowCallScreen(false)} />
//     </div>
//   );
// };

// export default CallButton;
